# Example Dockerfile.override for installing NetBox Maintenance Device plugin
# Copy this file to your netbox-docker directory for custom plugin installation
# Exemplo de Dockerfile.override para instalar o plugin NetBox Maintenance Device
# Copie este arquivo para o diretório netbox-docker para instalação personalizada de plugins

FROM netboxcommunity/netbox:latest

# Install the NetBox Maintenance Device plugin
# Instale o plugin NetBox Maintenance Device
RUN pip install netbox-maintenance-device

# Optional: Install additional dependencies if needed
# Opcional: Instale dependências adicionais se necessário
# RUN pip install additional-package

# The base image already handles the rest of the NetBox setup
# A imagem base já cuida do resto da configuração do NetBox